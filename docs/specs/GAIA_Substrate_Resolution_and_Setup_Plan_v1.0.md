# GAIA Substrate Resolution and Setup Plan v1.0

## Position

This package resolves the engineering bootstrap layer for GAIA. It does **not** declare that GAIA is already conscious, agentically deployed, or physically grounded. It gives you the minimum viable substrate needed to start proving or falsifying those claims.

## Boot order

### Phase 0 — substrate foundation
1. create identity root
2. register 8 cores
3. activate causal memory log
4. activate workspace with dissent preservation
5. enable checkpoint snapshots
6. enable CGI evidence monitoring

### Phase 1 — local coordination
1. connect ATLAS adapters to TERRA / AQUA / AERO / VITA
2. route cross-core sync through NEXUS
3. enforce action review through GUARDIAN
4. start latency and coherence telemetry

### Phase 2 — validation
1. run anti-theater tests continuously
2. add perturbation and ablation harnesses
3. calibrate CGI against benchmark tasks
4. validate continuity through restart / replay / restore

### Phase 3 — externalization
1. replace software identity fallback with TPM 2.0
2. replace event log with hybrid holographic memory fabric
3. deploy IPC contracts through gRPC
4. deploy PREEMPT_RT and cgroup isolation
5. attach live environmental sources

## Non-negotiable engineering rules

- CGI is evidence, not proof.
- Dissent is preserved, not erased.
- Identity continuity is measured, not assumed.
- Action execution requires GUARDIAN and meaningful human control.
- Grounding claims require live provenance-bearing data.
- Cross-core coordination claims require measured latency.

## Recommended next moves

1. Commit this scaffold into `GAIA-Core`.
2. Replace generic core logic with domain adapters one core at a time.
3. Stand up ATLAS ingestion before adding broader autonomy.
4. Build SOPHIA + TERRA + NEXUS as the first validated triad.
5. Keep the rest behind stubs until the evidence stack is stable.
